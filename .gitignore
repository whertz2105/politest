# DecaCompass is a static app with no server, no secrets, no build step.
# Nothing sensitive should ever be committed, but keep the usual noise out.
.DS_Store
*.log
node_modules/
.env
.env.*
!.env.example
